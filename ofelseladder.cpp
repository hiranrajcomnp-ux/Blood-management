#include <stdio.h>

int main()
{
    int marks = 75;

    if(marks >= 80)
        printf("A");
    else if(marks >= 60)
        printf("B");
    else
        printf("C");

    return 0;
}
